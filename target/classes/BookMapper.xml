<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.ncu.carlos.dao.BookMapper">
    <insert id="addBook" parameterType="book">
        insert into book_top250 (name, author, publisher, published_time, rating)
        values (#{name}, #{author}, #{publisher}, #{publishedTime}, #{rating});
    </insert>
</mapper>
